<h1> Categorias </h1>

<table>
  <% @categories.each do |form| %>
    <tr>
      <td><%= link_to form.name, category_path(form) %></td>
      <td><%= form.description %></td>
      <td><%= button_to 'Editar', edit_category_path(form), method: 'get'%></td>
      <td><%= button_to 'Remover', category_path(form), method: 'delete', data: { confirm: 'Você tem certeza?' } %></td>
  <% end %>
</table>
<p> <%= link_to "Adicionar nova categoria", new_category_path %> </p>
